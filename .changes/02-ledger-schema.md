# 02 — Ledger schema

Replaces legacy migrations with account classes/subtypes, immutable transactions and decimal FX
entries, system equity/categories, user currencies, and all 24 supported currency contracts.

Verify: deploy the clean migration and run contract/schema tests.
