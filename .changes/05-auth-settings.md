# 05 — Authentication and settings

Adds atomic normalized registration, session lifecycle, credential/account management, encrypted
TOTP/recovery codes, security headers, request IDs, health/readiness, and reporting preferences.

Verify: run auth/security and HTTP boundary tests.
