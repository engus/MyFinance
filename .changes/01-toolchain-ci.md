# 01 — Toolchain and CI

Adds reproducible workspace commands, strict no-emit TypeScript, formatting/linting, isolated
development/test PostgreSQL, GitHub Actions, dependency audit, and bootstrap documentation.

Verify: `npm run lint && npm run typecheck && npm run build`.
