# 03 — Ledger API

Adds typed income/expense/transfer/opening/liability operations, atomic reversal/correction,
currency locks, archives, filters, cursor pagination, and the common error envelope.

Verify: run ledger integrity tests and inspect reversal links.
